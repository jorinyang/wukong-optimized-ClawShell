#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
钉钉待办完整查询方案
对比 MCP 服务与原生 API 的差异
"""

import json

def analyze_todo_sources():
    """分析待办数据来源"""
    
    analysis = """
================================================================================
📊 钉钉待办数据来源分析
================================================================================

一、MCP 服务查询范围（当前可用）
--------------------------------------------------------------------------------
工具: get_user_todos_in_current_org
描述: "获取当前用户在所属组织中的个人待办事项列表"

✅ 可查询的待办类型:
   • 组织内标准待办 (source=teambition)
   • MCP 创建的待办
   • 工作流触发的待办

❌ 不可查询的待办类型:
   • 消息"稍后处理" (source=im)
   • 群聊消息转待办
   • 外部应用创建的待办

二、截图中缺失待办的原因
--------------------------------------------------------------------------------
根据截图显示，以下待办未被 MCP API 返回:

1. "推进 OpenClaw 项目进度" (已截止)
2. "联系张葳，推进兴义水务智慧仓库项目进度" (截止:明天)
3. "联系歪哥（151 8655 5531），沟通水库弱电项目相关事宜" (截止:明天)

这些待办的共同特征:
   🔍 来源: 消息"稍后处理"功能
   🔍 创建方式: 长按消息 → 稍后处理
   🔍 存储位置: 个人消息待办，非组织待办

三、解决方案
================================================================================

方案 1: 使用钉钉开放平台原生 API (推荐)
--------------------------------------------------------------------------------
API 端点: POST https://oapi.dingtalk.com/topapi/workrecord/task/getusertasklist

参数:
  • access_token: 应用访问令牌
  • userid: 用户ID
  • status: 0=未完成, 1=已完成
  • page_size: 分页大小
  • page_number: 页码

限制:
  ⚠️ 需要创建钉钉企业内部应用
  ⚠️ 需要申请待办相关权限
  ⚠️ 仍然可能无法查询"稍后处理"类待办

方案 2: 将消息待办转为标准待办
--------------------------------------------------------------------------------
操作步骤:
  1. 在钉钉中找到"稍后处理"的待办
  2. 点击进入详情
  3. 选择"转为标准待办"或手动创建

优点:
  ✅ 创建后即可被 MCP API 查询
  ✅ 统一在待办列表中管理

方案 3: 使用钉钉个人访问令牌 (实验性)
--------------------------------------------------------------------------------
某些内部 API 可能支持更广泛的查询，但:
  ⚠️ 官方文档未公开
  ⚠️ 可能随时变更
  ⚠️ 需要逆向工程获取

四、实际操作建议
================================================================================

当前最佳实践:

1. 对于重要待办:
   直接在钉钉中创建标准待办，而非使用"稍后处理"
   
2. 对于已有"稍后处理"待办:
   手动转为标准待办，或直接在钉钉中管理

3. 对于 MCP 待办管理:
   仅将 MCP 用于创建/查询标准待办
   不期望它能管理所有类型的待办

五、技术实现
================================================================================

已为您准备两个脚本:

1. dingtalk_todo_api.py
   使用钉钉开放平台原生 API 查询待办
   需要: AppKey, AppSecret, UserID

2. 当前 MCP 方式
   使用钉钉官方 MCP 服务
   已配置完成，可直接使用

================================================================================
"""
    print(analysis)


def compare_query_methods():
    """对比不同查询方式"""
    
    comparison = {
        "查询方式": ["MCP 服务", "钉钉原生 API", "钉钉 App 内"],
        "组织待办": ["✅ 支持", "✅ 支持", "✅ 支持"],
        "稍后处理": ["❌ 不支持", "❓ 可能支持", "✅ 支持"],
        "消息转待办": ["❌ 不支持", "❓ 可能支持", "✅ 支持"],
        "调用复杂度": ["简单", "中等", "N/A"],
        "配置要求": ["已配置完成", "需申请应用", "无需配置"]
    }
    
    print("\n📊 查询方式对比:\n")
    print(f"{'查询方式':<15} {'组织待办':<10} {'稍后处理':<10} {'消息转待办':<12} {'复杂度':<10} {'配置要求':<15}")
    print("-" * 80)
    for i in range(len(comparison["查询方式"])):
        print(f"{comparison['查询方式'][i]:<15} {comparison['组织待办'][i]:<10} {comparison['稍后处理'][i]:<10} {comparison['消息转待办'][i]:<12} {comparison['调用复杂度'][i]:<10} {comparison['配置要求'][i]:<15}")


if __name__ == "__main__":
    analyze_todo_sources()
    compare_query_methods()
    
    print("\n" + "="*80)
    print("💡 结论:")
    print("="*80)
    print("""
MCP 服务查询不完整是预期行为，因为:
1. MCP 仅查询"组织内"标准待办
2. 截图中的"稍后处理"待办属于个人消息范畴
3. 这是钉钉的产品设计，非技术故障

建议:
• 继续使用 MCP 管理标准待办
• 在钉钉中直接查看"稍后处理"类待办
• 如需统一管理，将重要待办转为标准待办
    """)
